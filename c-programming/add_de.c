#include <stdio.h>
/*
* this code is used to calculate the addition numbers and decimals
*/

int main()
{

	double decimal;
	int number;

	printf("Please enter what you want to calculate: ");
	scanf("%i + %f", &&number, decimal);

	double result = number + decimal;

	printf("the addition of %i number and %f decimal is %f\n", result);



return 0;
}
